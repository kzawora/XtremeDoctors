init - resets migrations and database. Use after cloning or changing models.

updateMigrations - adds new migration. Use when change model
updateDatabase - applies all migrations. Use after new migrations were done

resetMigrations - deletes all migrations and creates new
resetDatabase - deletes database and applies all migrations available
